node_modules/
dist/*
# Keep the single-file bundle tracked so it can be downloaded directly.
!dist/agenite.html
*.log
.DS_Store
.env
agenite.config.json

# Local tooling / temp verification scripts
.workbuddy/
